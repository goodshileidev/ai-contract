---
文档类型: 实现文档
需求编号: DOC-2025-11-001
创建日期: 2025-11-29
创建者: claude-sonnet-4-5 (claude-sonnet-4-5-20250929)
最后更新: 2025-11-29
更新者: claude-sonnet-4-5 (claude-sonnet-4-5-20250929)
状态: 已批准
---

# AI标书智能创作平台 - 后端开发阶段

> **开发周期**: 第4-6周（3周）
> **团队**: 后端工程师3人
> **目标**: 完成认证授权、用户管理、项目管理等核心后端功能

## 📋 文档导航

本文档是开发计划与路线图的一部分，其他相关文档：

1. [00-开发计划总览.md](./00-开发计划总览.md) - 项目阶段概览和团队配置
2. [01-准备与基础设施阶段.md](./01-准备与基础设施阶段.md) - 阶段0-1开发计划
3. **02-后端开发阶段.md**（本文档）- 阶段2后端核心开发
4. [03-前端开发阶段.md](./03-前端开发阶段.md) - 阶段3前端核心开发
5. [04-AI能力开发阶段.md](./04-AI能力开发阶段.md) - 阶段4 AI能力开发
6. [05-业务功能开发阶段.md](./05-业务功能开发阶段.md) - 阶段5业务功能开发
7. [06-测试部署与总结.md](./06-测试部署与总结.md) - 阶段6-7测试部署

## 阶段2：后端核心开发 (第4-6周)

**目标**: 完成认证授权、用户管理、项目管理等核心后端功能

### 任务 2.1: 认证授权系统

**负责人**: 后端工程师 (安全专员)
**工时**: 24小时
**优先级**: P0

**详细步骤**:

1. **密码加密工具** (2h)
   - [ ] 创建app/core/security.py:
     ```python
     from passlib.context import CryptContext
     from jose import jwt
     from datetime import datetime, timedelta

     pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

     def verify_password(plain_password: str, hashed_password: str) -> bool:
         return pwd_context.verify(plain_password, hashed_password)

     def get_password_hash(password: str) -> str:
         return pwd_context.hash(password)

     def create_access_token(data: dict, expires_delta: timedelta = None):
         to_encode = data.copy()
         expire = datetime.utcnow() + (expires_delta or timedelta(minutes=30))
         to_encode.update({"exp": expire, "type": "access"})
         return jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.ALGORITHM)

     def create_refresh_token(data: dict):
         to_encode = data.copy()
         expire = datetime.utcnow() + timedelta(days=7)
         to_encode.update({"exp": expire, "type": "refresh"})
         return jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.ALGORITHM)
     ```
   - [ ] 测试密码加密和验证
   - [ ] 测试Token生成

2. **用户Schema定义** (3h)
   - [ ] 创建app/schemas/user.py:
     ```python
     from pydantic import BaseModel, EmailStr, Field
     from typing import Optional
     from datetime import datetime

     class UserBase(BaseModel):
         email: EmailStr
         username: str = Field(..., min_length=3, max_length=50)
         full_name: Optional[str] = None

     class UserCreate(UserBase):
         password: str = Field(..., min_length=8)

     class UserUpdate(BaseModel):
         full_name: Optional[str] = None
         phone: Optional[str] = None
         avatar_url: Optional[str] = None

     class UserInDB(UserBase):
         id: str
         organization_id: Optional[str]
         status: str
         created_at: datetime

         class Config:
             from_attributes = True

     class UserResponse(UserInDB):
         roles: list[str] = []
     ```
   - [ ] 创建登录Schema
   - [ ] 创建注册Schema
   - [ ] 添加字段验证规则

3. **用户CRUD操作** (4h)
   - [ ] 创建app/crud/user.py:
     ```python
     from sqlalchemy.ext.asyncio import AsyncSession
     from sqlalchemy import select
     from app.models.user import User
     from app.schemas.user import UserCreate, UserUpdate
     from app.core.security import get_password_hash

     class CRUDUser:
         async def get(self, db: AsyncSession, id: str) -> Optional[User]:
             result = await db.execute(select(User).where(User.id == id))
             return result.scalar_one_or_none()

         async def get_by_email(self, db: AsyncSession, email: str) -> Optional[User]:
             result = await db.execute(
                 select(User).where(User.email == email, User.deleted_at.is_(None))
             )
             return result.scalar_one_or_none()

         async def create(self, db: AsyncSession, obj_in: UserCreate) -> User:
             db_obj = User(
                 email=obj_in.email,
                 username=obj_in.username,
                 full_name=obj_in.full_name,
                 hashed_password=get_password_hash(obj_in.password),
             )
             db.add(db_obj)
             await db.commit()
             await db.refresh(db_obj)
             return db_obj

         async def update(self, db: AsyncSession, db_obj: User, obj_in: UserUpdate) -> User:
             for field, value in obj_in.dict(exclude_unset=True).items():
                 setattr(db_obj, field, value)
             await db.commit()
             await db.refresh(db_obj)
             return db_obj

     user = CRUDUser()
     ```
   - [ ] 实现所有CRUD方法
   - [ ] 添加软删除支持
   - [ ] 添加分页查询

4. **认证依赖项** (3h)
   - [ ] 创建app/api/dependencies/auth.py:
     ```python
     from fastapi import Depends, HTTPException, status
     from fastapi.security import OAuth2PasswordBearer
     from jose import jwt, JWTError
     from sqlalchemy.ext.asyncio import AsyncSession

     oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")

     async def get_current_user(
         token: str = Depends(oauth2_scheme),
         db: AsyncSession = Depends(get_db)
     ) -> User:
         credentials_exception = HTTPException(
             status_code=status.HTTP_401_UNAUTHORIZED,
             detail="Could not validate credentials",
             headers={"WWW-Authenticate": "Bearer"},
         )

         try:
             payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
             user_id: str = payload.get("sub")
             if user_id is None:
                 raise credentials_exception
         except JWTError:
             raise credentials_exception

         user = await crud.user.get(db, id=user_id)
         if user is None:
             raise credentials_exception

         return user

     async def get_current_active_user(
         current_user: User = Depends(get_current_user)
     ) -> User:
         if current_user.status != "active":
             raise HTTPException(status_code=400, detail="Inactive user")
         return current_user
     ```
   - [ ] 实现权限检查依赖项
   - [ ] 实现角色检查依赖项

5. **认证API端点** (8h)
   - [ ] 创建app/api/v1/endpoints/auth.py:
     ```python
     from fastapi import APIRouter, Depends, HTTPException, status
     from fastapi.security import OAuth2PasswordRequestForm
     from sqlalchemy.ext.asyncio import AsyncSession

     router = APIRouter()

     @router.post("/register", response_model=Response[UserResponse])
     async def register(
         user_in: UserCreate,
         db: AsyncSession = Depends(get_db)
     ):
         """用户注册"""
         # 检查邮箱是否已存在
         user = await crud.user.get_by_email(db, email=user_in.email)
         if user:
             raise HTTPException(
                 status_code=400,
                 detail="Email already registered"
             )

         # 创建用户
         user = await crud.user.create(db, obj_in=user_in)

         # 分配默认角色
         await crud.user.add_role(db, user_id=user.id, role_name="user")

         return Response(data=UserResponse.from_orm(user))

     @router.post("/login", response_model=Response[dict])
     async def login(
         form_data: OAuth2PasswordRequestForm = Depends(),
         db: AsyncSession = Depends(get_db)
     ):
         """用户登录"""
         user = await crud.user.authenticate(
             db, email=form_data.username, password=form_data.password
         )

         if not user:
             raise HTTPException(
                 status_code=status.HTTP_401_UNAUTHORIZED,
                 detail="Incorrect email or password"
             )

         # 生成Token
         access_token = create_access_token(data={"sub": str(user.id)})
         refresh_token = create_refresh_token(data={"sub": str(user.id)})

         # 保存refresh token到Redis
         await redis_client.set(
             f"refresh_token:{user.id}",
             refresh_token,
             expire=7 * 24 * 3600
         )

         return Response(data={
             "access_token": access_token,
             "refresh_token": refresh_token,
             "token_type": "bearer",
             "expires_in": 1800,
             "user": UserResponse.from_orm(user)
         })

     @router.post("/refresh", response_model=Response[dict])
     async def refresh_token(
         refresh_token: str,
         db: AsyncSession = Depends(get_db)
     ):
         """刷新Token"""
         # 验证refresh token
         # 生成新的access token
         # 返回新token
         pass

     @router.post("/logout")
     async def logout(
         current_user: User = Depends(get_current_active_user)
     ):
         """用户登出"""
         # 从Redis删除refresh token
         await redis_client.delete(f"refresh_token:{current_user.id}")
         return Response(data={"message": "Successfully logged out"})

     @router.get("/me", response_model=Response[UserResponse])
     async def get_current_user_info(
         current_user: User = Depends(get_current_active_user)
     ):
         """获取当前用户信息"""
         return Response(data=UserResponse.from_orm(current_user))
     ```
   - [ ] 实现注册逻辑
   - [ ] 实现登录逻辑
   - [ ] 实现Token刷新
   - [ ] 实现登出逻辑

6. **单元测试** (4h)
   - [ ] 创建tests/test_auth.py
   - [ ] 测试密码加密
   - [ ] 测试Token生成和验证
   - [ ] 测试注册流程
   - [ ] 测试登录流程
   - [ ] 测试权限验证

**交付物**:
- 完整的认证授权系统
- 用户CRUD操作
- 认证API端点
- 单元测试

**验收标准**:
- 用户能够成功注册
- 用户能够成功登录并获取Token
- Token验证正常工作
- 受保护的端点需要认证
- 所有测试通过

---

### 任务 2.2: 用户管理API

**负责人**: 后端工程师
**工时**: 16小时
**依赖**: 任务2.1

**详细步骤**:

1. **用户列表查询** (4h)
   - [ ] 实现分页查询
   - [ ] 实现筛选（状态、角色、组织）
   - [ ] 实现搜索（邮箱、姓名、用户名）
   - [ ] 实现排序
   - [ ] 添加性能优化（索引、查询优化）

2. **用户详情获取** (2h)
   - [ ] 实现用户详情查询
   - [ ] 包含角色信息
   - [ ] 包含组织信息
   - [ ] 包含权限列表

3. **用户信息更新** (3h)
   - [ ] 实现个人信息更新
   - [ ] 实现头像上传
   - [ ] 实现密码修改
   - [ ] 添加字段验证

4. **用户状态管理** (3h)
   - [ ] 实现用户激活/停用
   - [ ] 实现用户删除（软删除）
   - [ ] 实现批量操作
   - [ ] 添加操作日志

5. **角色权限管理** (4h)
   - [ ] 实现用户角色分配
   - [ ] 实现用户权限查询
   - [ ] 实现权限检查中间件
   - [ ] 添加角色缓存

**交付物**:
- 用户管理完整API
- 角色权限系统
- API测试用例

**验收标准**:
- 所有用户管理API正常工作
- 权限控制正确
- 性能满足要求（<100ms）

---

### 任务 2.3: 组织管理API

**负责人**: 后端工程师
**工时**: 12小时
**依赖**: 任务2.2

**详细步骤**:

1. **组织Schema定义** (2h)
   - [ ] 创建OrganizationCreate
   - [ ] 创建OrganizationUpdate
   - [ ] 创建OrganizationResponse
   - [ ] 添加字段验证

2. **组织CRUD** (4h)
   - [ ] 实现创建组织
   - [ ] 实现更新组织信息
   - [ ] 实现组织查询
   - [ ] 实现组织删除

3. **组织成员管理** (4h)
   - [ ] 实现添加成员
   - [ ] 实现移除成员
   - [ ] 实现成员列表查询
   - [ ] 实现成员角色管理

4. **组织配置** (2h)
   - [ ] 实现组织设置
   - [ ] 实现配额管理
   - [ ] 实现计费信息

**交付物**:
- 组织管理API
- 成员管理API
- 测试用例

**验收标准**:
- 组织CRUD正常工作
- 成员管理正常
- 权限隔离正确

---

### 任务 2.4: 项目管理API

**负责人**: 后端工程师
**工时**: 20小时
**依赖**: 任务2.3

**详细步骤**:

1. **项目Schema定义** (3h)
   - [ ] 创建ProjectCreate Schema
   - [ ] 创建ProjectUpdate Schema
   - [ ] 创建ProjectResponse Schema
   - [ ] 创建ProjectList Schema
   - [ ] 添加复杂字段验证（预算、日期等）

2. **项目CRUD实现** (6h)
   - [ ] 创建app/crud/project.py
   - [ ] 实现创建项目:
     ```python
     async def create(
         self,
         db: AsyncSession,
         obj_in: ProjectCreate,
         created_by: str
     ) -> Project:
         db_obj = Project(
             **obj_in.dict(),
             created_by=created_by,
             code=self.generate_project_code()
         )
         db.add(db_obj)
         await db.commit()
         await db.refresh(db_obj)

         # 自动添加创建者为项目成员
         await self.add_member(db, project_id=db_obj.id, user_id=created_by, role="owner")

         return db_obj
     ```
   - [ ] 实现更新项目
   - [ ] 实现查询项目（含过滤、排序、分页）
   - [ ] 实现删除项目（软删除）
   - [ ] 实现项目统计

3. **项目成员管理** (5h)
   - [ ] 实现添加成员API
   - [ ] 实现移除成员API
   - [ ] 实现成员权限管理
   - [ ] 实现成员列表查询
   - [ ] 添加成员变更通知

4. **项目API端点** (4h)
   - [ ] POST /api/v1/projects (创建项目)
   - [ ] GET /api/v1/projects (项目列表)
   - [ ] GET /api/v1/projects/{id} (项目详情)
   - [ ] PUT /api/v1/projects/{id} (更新项目)
   - [ ] DELETE /api/v1/projects/{id} (删除项目)
   - [ ] POST /api/v1/projects/{id}/members (添加成员)
   - [ ] DELETE /api/v1/projects/{id}/members/{userId} (移除成员)

5. **单元测试** (2h)
   - [ ] 测试项目创建
   - [ ] 测试项目查询
   - [ ] 测试项目更新
   - [ ] 测试权限控制
   - [ ] 测试成员管理

**交付物**:
- 项目管理完整API
- 项目成员管理API
- 单元测试

**验收标准**:
- 项目CRUD正常工作
- 成员管理功能正常
- 权限控制正确
- 所有测试通过

---

### 任务 2.5: 招标文件管理API

**负责人**: 后端工程师
**工时**: 16小时
**依赖**: 任务2.4

**详细步骤**:

1. **文件上传服务** (4h)
   - [ ] 配置MinIO客户端
   - [ ] 实现文件上传到MinIO
   - [ ] 实现文件下载
   - [ ] 实现文件删除
   - [ ] 添加文件类型验证
   - [ ] 添加文件大小限制（100MB）

2. **招标文件Schema** (2h)
   - [ ] 创建BiddingDocumentCreate
   - [ ] 创建BiddingDocumentResponse
   - [ ] 添加文件元数据字段

3. **招标文件CRUD** (5h)
   - [ ] 实现上传招标文件
   - [ ] 实现文件列表查询
   - [ ] 实现文件详情获取
   - [ ] 实现文件删除
   - [ ] 实现文件下载URL生成

4. **文件解析服务** (3h)
   - [ ] 实现PDF文本提取
   - [ ] 实现Word文档解析
   - [ ] 实现文件内容存储
   - [ ] 添加解析状态跟踪

5. **API端点实现** (2h)
   - [ ] POST /api/v1/projects/{id}/bidding-documents (上传文件)
   - [ ] GET /api/v1/projects/{id}/bidding-documents (文件列表)
   - [ ] GET /api/v1/bidding-documents/{id} (文件详情)
   - [ ] DELETE /api/v1/bidding-documents/{id} (删除文件)
   - [ ] GET /api/v1/bidding-documents/{id}/download (下载文件)

**交付物**:
- 文件上传服务
- 招标文件管理API
- 文件解析服务

**验收标准**:
- 文件能够成功上传到MinIO
- 文件列表正常显示
- 文件下载正常
- PDF和Word解析正常

## 🔗 相关文档

- **开发计划总览**: [00-开发计划总览.md](./00-开发计划总览.md)
- **准备与基础设施**: [01-准备与基础设施阶段.md](./01-准备与基础设施阶段.md)
- **前端开发**: [03-前端开发阶段.md](./03-前端开发阶段.md)
- **AI能力开发**: [04-AI能力开发阶段.md](./04-AI能力开发阶段.md)

## 修改历史

| 日期 | 版本 | 修改者 | 修改内容概要 |
|------|------|--------|-------------|
| 2025-11-29 | 1.0 | claude-sonnet-4-5 (claude-sonnet-4-5-20250929) | 从开发计划与路线图.md拆分创建后端开发阶段文档 |

---

**文档版本**: v1.0
**创建时间**: 2025年11月29日
**文档状态**: ✅ 已批准
