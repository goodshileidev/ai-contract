---
文档类型: 架构文档
需求编号: DOC-2025-11-001
创建日期: 2025-11-29
创建者: claude-sonnet-4-5 (claude-sonnet-4-5-20250929)
最后更新: 2025-11-30 00:25
updater: gemini-pro
status: 已批准
---

# AI标书智能创作平台 - 后端服务架构

> **架构模式**: 混合后端双服务架构
> **Java服务**: Spring Boot 3.2 (端口8080) - 业务数据维护
> **Python服务**: FastAPI 0.104 (端口8001) - AI能力服务
> **服务通信**: HTTP REST API + RabbitMQ消息队列

## 📋 文档导航

本文档是技术架构设计的一部分,其他相关文档:

1. [00-技术架构总览.md](./00-技术架构总览.md) - 整体架构和前端技术架构
2. **01-后端服务架构.md**(本文档)- Java和Python服务架构详解
3. [02-AI能力架构概述.md](./02-AI能力架构概述.md) - AI服务架构设计概述
4. [03-数据存储架构.md](./03-数据存储架构.md) - 数据存储和消息队列架构
5. [04-部署运维架构.md](./04-部署运维架构.md) - 部署、安全和监控架构

## ⚙️ 后端服务架构

### 混合后端架构设计
```yaml
# 双服务架构
services:
  # Java业务服务(Spring Boot 3.2)
  java-service:
    port: 8080
    description: "数据维护服务,处理业务逻辑和数据持久化"
    modules:
      - user-service:          # 用户管理
          database: PostgreSQL
          features:
            - 用户认证授权
            - 角色权限管理
            - 用户资料管理

      - project-service:       # 项目管理
          database: PostgreSQL
          features:
            - 项目CRUD
            - 项目成员管理
            - 项目状态流转

      - template-service:      # 模板管理
          database: PostgreSQL
          features:
            - 模板CRUD
            - 模板版本管理
            - 模板分类管理

      - document-service:      # 文档管理
          database: PostgreSQL
          fileStorage: MinIO
          features:
            - 文档存储
            - 版本控制
            - 权限管理

      - collaboration-service: # 协作管理
          database: PostgreSQL
          features:
            - 基于流程流转的协作
            - 评论审批
            - 通知推送

      - export-service:       # 导出服务
          queue: Redis
          features:
            - 文档导出
            - 格式转换
            - 批量处理

  # Python AI服务(FastAPI 0.104)
  python-ai-service:
    port: 8001
    description: "AI能力服务,提供智能分析和内容生成"
    modules:
      - llm-service:          # LLM服务
          models:
            - OpenAI GPT-4
            - Anthropic Claude
            - 智谱AI

      - rag-service:          # RAG服务
          framework: LlamaIndex 0.9+
          vectorDB: Elasticsearch 8+ (主力，用于全文/向量检索)
          features:
            - 文档向量化
            - 语义检索
            - 上下文增强

      - analysis-service:     # 分析服务
          features:
            - 招标文档分析
            - 需求提取
            - 风险评估
            - 知识图谱查询与分析

      - generation-service:   # 生成服务
          features:
            - 标书内容生成
            - 智能推荐
            - 质量评估

      - workflow-service:     # 工作流编排
          framework: LangChain 0.1+
          features:
            - 任务编排
            - 流程管理
            - 状态跟踪
```

### 服务间通信设计
```java
// Java服务调用Python AI服务
@Service
public class AIServiceClient {

    @Value("${ai.service.url}")
    private String aiServiceUrl; // http://python-ai-service:8001

    private final RestTemplate restTemplate;
    private final WebClient webClient;

    /**
     * 分析招标文档
     */
    public DocumentAnalysisResponse analyzeTenderDocument(String documentId, String content) {
        String url = aiServiceUrl + "/api/v1/analyze/document";

        DocumentAnalysisRequest request = DocumentAnalysisRequest.builder()
            .documentId(documentId)
            .documentContent(content)
            .documentType("tender")
            .analysisOptions(List.of("requirements", "risks", "opportunities"))
            .build();

        return webClient.post()
            .uri(url)
            .bodyValue(request)
            .retrieve()
            .bodyToMono(DocumentAnalysisResponse.class)
            .block();
    }

    /**
     * 生成标书内容
     */
    public ContentGenerationResponse generateBidContent(
        String projectId,
        String templateId,
        Map<String, Object> requirements,
        CompanyProfile companyProfile
    ) {
        String url = aiServiceUrl + "/api/v1/generate/content";

        ContentGenerationRequest request = ContentGenerationRequest.builder()
            .projectId(projectId)
            .templateId(templateId)
            .requirements(requirements)
            .companyProfile(companyProfile.toMap())
            .build();

        return webClient.post()
            .uri(url)
            .bodyValue(request)
            .retrieve()
            .bodyToMono(ContentGenerationResponse.class)
            .block();
    }
}
```

### API设计规范
```typescript
// RESTful API设计
interface APIStandards {
  // 基础URL结构
  javaServiceUrl: "http://java-service:8080/api/v1";
  pythonServiceUrl: "http://python-ai-service:8001/api/v1";

  // Java服务资源(数据维护)
  javaResources: {
    users: "/users";
    projects: "/projects";
    templates: "/templates";
    documents: "/documents";
    collaboration: "/collaboration";
  };

  // Python服务资源(AI能力)
  pythonResources: {
    analyze: "/analyze";
    generate: "/generate";
    match: "/match";
    optimize: "/optimize";
  };

  // HTTP方法使用
  methods: {
    GET: "获取资源";
    POST: "创建资源";
    PUT: "完整更新资源";
    PATCH: "部分更新资源";
    DELETE: "删除资源";
  };

  // 响应格式
  responseFormat: {
    success: {
      data: "any";
      message: "string";
      timestamp: "ISO8601";
    };
    error: {
      error: {
        code: "string";
        message: "string";
        details: "any";
      };
      timestamp: "ISO8601";
    };
  };
}

// Java服务API示例
interface ProjectAPI {
  // 项目列表(Java服务)
  "GET /api/v1/projects": {
    query?: {
      page?: number;
      limit?: number;
      status?: ProjectStatus;
      search?: string;
    };
    response: PaginatedResponse<Project>;
  };

  // 创建项目(Java服务)
  "POST /api/v1/projects": {
    body: CreateProjectRequest;
    response: Project;
  };
}

// Python AI服务API示例
interface AIServiceAPI {
  // 文档分析(Python服务)
  "POST /api/v1/analyze/document": {
    body: {
      documentId: string;
      documentContent: string;
      documentType: string;
      analysisOptions: string[];
    };
    response: DocumentAnalysisResponse;
  };

  // 内容生成(Python服务)
  "POST /api/v1/generate/content": {
    body: {
      projectId: string;
      templateId: string;
      requirements: object;
      companyProfile: object;
    };
    response: ContentGenerationResponse;
  };
}
```

## 🔗 相关文档

- **技术架构总览**: [00-技术架构总览.md](./00-技术架构总览.md)
- **AI能力架构**: [02-AI能力架构概述.md](./02-AI能力架构概述.md)
- **AI能力层详细设计**: [@docs/03-架构设计/05-AI能力层设计/](../05-AI能力层设计/)
- **数据存储架构**: [03-数据存储架构.md](./03-数据存储架构.md)
- **部署运维架构**: [04-部署运维架构.md](./04-部署运维架构.md)

## 修改历史

| 日期 | 版本 | 修改者 | 修改内容概要 |
|------|------|--------|-------------|
| 2025-11-30 00:25 | 1.1 | gemini-pro | 根据最新技术决策调整后端服务架构：明确Elasticsearch为向量检索主力，协作服务调整为流程流转，并增加知识图谱查询与分析功能。 |
| 2025-11-29 | 1.0 | claude-sonnet-4-5 (claude-sonnet-4-5-20250929) | 从02-技术架构设计.md拆分创建后端服务架构文档 |

---

**文档版本**: v1.0
**创建时间**: 2025年11月29日
**文档状态**: ✅ 已批准
