# 修复建议

## 需要修复的文件


### structured-requirements/individual-jsons/9-数据实体/AUDIT_LOGS_PARTITIONED-audit_logs_partitioned.json

**Schema**: entity


**额外字段**:
- `has_soft_delete`: 不在schema定义中，建议删除或更新schema
- `has_audit_fields`: 不在schema定义中，建议删除或更新schema
- `domain`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_base_entity`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- entity_name: 'audit_logs_partitioned' is too long
- entity_key: 'AUDIT_LOGS_PARTITIONED' is too long
- entity_title: 'audit_logs_partitioned' is too long
- entity_title_cn: 'audit_logs_partitioned' is too long
- entity_title_en: 'audit_logs_partitioned' is too long
- entity_title_jp: 'audit_logs_partitioned' is too long

### structured-requirements/individual-jsons/9-数据实体/COLLABORATION_SESSIONS-协作会话表.json

**Schema**: entity


**额外字段**:
- `has_soft_delete`: 不在schema定义中，建议删除或更新schema
- `has_audit_fields`: 不在schema定义中，建议删除或更新schema
- `domain`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_base_entity`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- entity_key: 'COLLABORATION_SESSIONS' is too long
- entity_title_en: 'collaboration_sessions' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-average_cost.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-average_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-is_active.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-model_params.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '模型参数(temperature, max_tokens等)' is too long
- field_label_cn: '模型参数(temperature, max_tokens等)' is too long
- field_label_jp: '模型参数(temperature, max_tokens等)' is too long
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-prompt_template.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-system_prompt.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-tags.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-usage_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_prompts-variables.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-completed_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-completion_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-cost.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-duration_seconds.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-error_message.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-input_data.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-max_retries.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-output_data.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-priority.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-project_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-prompt_template.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-prompt_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-retry_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-started_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-task_type.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '任务类型: parse-解析, analyze-分析, match-匹配, generate-生成, review-审核' is too long
- field_label_cn: '任务类型: parse-解析, analyze-分析, match-匹配, generate-生成, review-审核' is too long
- field_label_jp: '任务类型: parse-解析, analyze-分析, match-匹配, generate-生成, review-审核' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-total_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_tasks-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-completion_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-cost.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-latency_ms.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-prompt_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-task_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-total_tokens.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/ai_usage_logs-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-comments.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-task_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_logs-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-assignee_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-comments.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-completed_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-deadline.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-step_number.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_tasks-workflow_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-definition.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-is_active.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-is_default.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/approval_workflows-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-changes.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-details.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-error_message.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-ip_address.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-resource_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-user_agent.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/audit_logs_partitioned-LIKE.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-last_edited_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-last_edited_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-locked_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-locked_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-page_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-plain_content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-project_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-template_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-toc.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bid_documents-word_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-file_path.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-file_size.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-parse_error.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-parsed_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-parsed_content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-project_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/bidding_documents-uploaded_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-certificate_url.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-expiry_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-is_valid.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-issue_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-scope.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/certifications-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_events-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_events-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_events-event_data.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_events-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_events-session_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_events-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-active_users.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-awareness_state.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '用户感知状态(Yjs Awareness)' is too long
- field_label_cn: '用户感知状态(Yjs Awareness)' is too long
- field_label_jp: '用户感知状态(Yjs Awareness)' is too long
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-cursor_positions.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-ended_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-is_active.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-last_activity_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-selections.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/collaboration_sessions-started_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-annual_revenue.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-brief_introduction.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-certifications.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-competitive_advantages.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- entity_field_name: 'competitive_advantages' is too long
- entity_field_key: 'COMPETITIVE_ADVANTAGES' is too long
- field_label: 'competitive_advantages' is too long
- field_label_cn: 'competitive_advantages' is too long
- field_label_en: 'competitive_advantages' is too long
- field_label_jp: 'competitive_advantages' is too long
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-core_business.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-core_competencies.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-employee_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-honors.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-key_technologies.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-major_clients.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-patents_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-rd_personnel_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/company_profiles-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-is_resolved.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-parent_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-position_data.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-resolved_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-resolved_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-section_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-selection_end.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-selection_start.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_comments-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-generation_prompt.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-is_generated.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-is_required.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-level.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-order_index.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-parent_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_sections-word_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-change_summary.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-plain_content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-version_number.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/document_versions-word_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-bid_amount.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-bidding_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-contract_amount.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-is_reusable.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-key_points.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-lessons_learned.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-submission_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-success_factors.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-tags.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/historical_bids-win_rate.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-address.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-established_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-logo_url.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-organization_type.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '组织类型: company-公司, government-政府, institution-事业单位, individual-个人' is too long
- field_label_cn: '组织类型: company-公司, government-政府, institution-事业单位, individual-个人' is too long
- field_label_jp: '组织类型: company-公司, government-政府, institution-事业单位, individual-个人' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-scale.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '企业规模: small-小型, medium-中型, large-大型, xlarge-超大型' is too long
- field_label_cn: '企业规模: small-小型, medium-中型, large-大型, xlarge-超大型' is too long
- field_label_jp: '企业规模: small-小型, medium-中型, large-大型, xlarge-超大型' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-settings.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/organizations-website.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-action.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '操作: create, read, update, delete, execute' is too long
- field_label_cn: '操作: create, read, update, delete, execute' is too long
- field_label_jp: '操作: create, read, update, delete, execute' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-is_system.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/permissions-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-certifications.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-is_available.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-project_experience_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- entity_field_name: 'project_experience_count' is too long
- entity_field_key: 'PROJECT_EXPERIENCE_COUNT' is too long
- field_label: 'project_experience_count' is too long
- field_label_cn: 'project_experience_count' is too long
- field_label_en: 'project_experience_count' is too long
- field_label_jp: 'project_experience_count' is too long
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-resume_url.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-skills.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-specialties.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/personnel-years_of_experience.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-advantages.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-application_scenarios.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- entity_field_name: 'application_scenarios' is too long
- entity_field_key: 'APPLICATION_SCENARIOS' is too long
- field_label: 'application_scenarios' is too long
- field_label_cn: 'application_scenarios' is too long
- field_label_en: 'application_scenarios' is too long
- field_label_jp: 'application_scenarios' is too long
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-documents.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-features.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-images.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-is_active.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-specifications.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-tags.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-technology_stack.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/products_services-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-achievements.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-challenges.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-contract_amount.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-customer_satisfaction.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- entity_field_name: 'customer_satisfaction' is too long
- entity_field_key: 'CUSTOMER_SATISFACTION' is too long
- field_label_en: 'customer_satisfaction' is too long
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-documents.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-duration_months.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-end_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-images.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-is_public.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-is_reference.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-project_description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-solutions.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-start_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-tags.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-team_size.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-technologies_used.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_cases-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-joined_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-permissions.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-project_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-role.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '项目角色: owner-所有者, manager-管理员, member-成员, viewer-查看者' is too long
- field_label_cn: '项目角色: owner-所有者, manager-管理员, member-成员, viewer-查看者' is too long
- field_label_jp: '项目角色: owner-所有者, manager-管理员, member-成员, viewer-查看者' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/project_members-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-is_mandatory.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-match_details.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-match_score.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-project_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-score_weight.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-source_page.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/project_requirements-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-budget_amount.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-end_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-settings.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-start_date.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-submission_deadline.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-tags.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/projects-win_probability.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/role_permissions-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/role_permissions-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/role_permissions-permission_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/role_permissions-role_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-is_system.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-level.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-permissions.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/roles-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/system_logs-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/system_logs-details.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/system_logs-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/system_logs-ip_address.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/system_logs-message.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/system_logs-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-content_template.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-hints.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-is_editable.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-is_required.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-level.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-order_index.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-parent_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-template_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_sections-variables.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-document_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-project_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-template_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-used_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/template_usage_logs-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-content.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-created_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-description.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-is_active.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-placeholders.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-preview_url.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-rating.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-rating_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-scope.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '作用域: public-公开, organization-组织, private-私有' is too long
- field_label_cn: '作用域: public-公开, organization-组织, private-私有' is too long
- field_label_jp: '作用域: public-公开, organization-组织, private-私有' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/templates-structure.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-tags.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-thumbnail_url.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-updated_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-usage_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/templates-variables.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-expires_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-granted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-granted_by.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-role_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/user_roles-user_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-avatar_url.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-created_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-deleted_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-email_verified.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-last_login_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-last_login_ip.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-login_count.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-metadata.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-organization_id.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-phone_verified.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-settings.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'

### structured-requirements/individual-jsons/9-数据实体/字段/users-status.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- field_label: '用户状态: active-活跃, inactive-未激活, suspended-已暂停' is too long
- field_label_cn: '用户状态: active-活跃, inactive-未激活, suspended-已暂停' is too long
- field_label_jp: '用户状态: active-活跃, inactive-未激活, suspended-已暂停' is too long

### structured-requirements/individual-jsons/9-数据实体/字段/users-updated_at.json

**Schema**: entity_field


**额外字段**:
- `entity_name`: 不在schema定义中，建议删除或更新schema
- `length`: 不在schema定义中，建议删除或更新schema
- `field_name_cn`: 不在schema定义中，建议删除或更新schema
- `field_name`: 不在schema定义中，建议删除或更新schema
- `db_type`: 不在schema定义中，建议删除或更新schema
- `field_type`: 不在schema定义中，建议删除或更新schema
- `version`: 不在schema定义中，建议删除或更新schema
- `is_not_null`: 不在schema定义中，建议删除或更新schema
- `description`: 不在schema定义中，建议删除或更新schema
- `table_name`: 不在schema定义中，建议删除或更新schema
- `is_unique`: 不在schema定义中，建议删除或更新schema

**验证错误**:
- max_length: None is not of type 'integer'