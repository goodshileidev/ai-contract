# AIBidComposer - 服务启动验证指南 - 性能基准

### 预期性能指标

#### 启动时间
- PostgreSQL: < 10秒
- Elasticsearch: < 30秒
- Redis: < 5秒
- RabbitMQ: < 15秒
- MinIO: < 10秒
- Java服务: < 60秒
- Python AI服务: < 20秒
- 前端服务: < 10秒

#### 健康检查响应时间
- PostgreSQL: < 100ms
- Elasticsearch: < 200ms
- Redis: < 50ms
- Java API: < 200ms
- Python AI API: < 300ms

#### 资源使用（开发环境）
- PostgreSQL: ~200MB内存
- Elasticsearch: ~2GB内存
- Redis: ~50MB内存
- RabbitMQ: ~150MB内存
- MinIO: ~100MB内存
- Java服务: ~512MB内存
- Python AI服务: ~256MB内存
- 前端服务: ~100MB内存

---
