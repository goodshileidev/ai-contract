---
文档类型: 架构文档
需求编号: DOC-2025-11-001
创建日期: 2025-11-29
创建者: claude-sonnet-4-5 (claude-sonnet-4-5-20250929)
最后更新: 2025-11-29
更新者: claude-sonnet-4-5 (claude-sonnet-4-5-20250929)
状态: 已批准
---

# AI标书智能创作平台 - 部署架构总览

> **部署架构**: 混合后端双服务部署架构
> **Java服务**: Spring Boot (端口 8080) - 核心业务逻辑
> **Python服务**: FastAPI (端口 8001) - AI能力服务
> **服务通信**: HTTP REST API + RabbitMQ消息队列
> **容器化**: Docker + Kubernetes

## 📋 文档导航

本目录包含部署架构的详细设计文档，已按功能模块拆分为以下文件：

1. **00-部署架构总览.md**（本文档）- 部署架构概览和环境规划
2. **01-Docker配置详解.md** - Docker镜像构建和Docker Compose配置
3. **02-Kubernetes配置.md** - Kubernetes完整部署配置
4. **03-部署运维脚本.md** - 部署脚本、监控日志和安全配置

## 📋 部署概览

### 部署环境

```yaml
环境规划:
  开发环境 (Development):
    用途: 本地开发和调试
    部署方式: Docker Compose
    规模: 单机部署
    域名: dev.aibidcomposer.local

  测试环境 (Testing):
    用途: 功能测试、集成测试
    部署方式: Kubernetes (单节点)
    规模: 小规模（2C4G）
    域名: test.aibidcomposer.com

  预发环境 (Staging):
    用途: 性能测试、UAT测试
    部署方式: Kubernetes (多节点)
    规模: 生产同等配置
    域名: staging.aibidcomposer.com

  生产环境 (Production):
    用途: 正式服务
    部署方式: Kubernetes (高可用集群)
    规模: 根据负载动态扩展
    域名: www.aibidcomposer.com
```

### 部署架构

```
生产环境拓扑（混合后端架构）:

                    ┌─────────────────────┐
                    │   DNS / CDN         │
                    │   (CloudFlare/AWS)  │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  Load Balancer      │
                    │  (Nginx/ALB/SLB)   │
                    └──────────┬──────────┘
                               │
┌──────────────────────────────▼────────────────────────────────┐
│                    Kubernetes Cluster                          │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Ingress Controller (Nginx/Traefik)                    │  │
│  └────────────────────┬───────────────────────────────────┘  │
│                       │                                        │
│  ┌────────────────────▼───────────────────────────────────┐  │
│  │  API Gateway (Kong)                                     │  │
│  │  - Authentication & Authorization                       │  │
│  │  - Rate Limiting & Throttling                           │  │
│  │  - Request Routing (Java/Python服务分发)               │  │
│  │  - Load Balancing                                        │  │
│  └────────────────────┬───────────────────────────────────┘  │
│                       │                                        │
│  ┌────────────────────▼───────────────────────────────────┐  │
│  │  Application Services (Pods)                            │  │
│  │                                                          │  │
│  │  Java服务 (Spring Boot, Port 8080):                    │  │
│  │  ├─ Backend API Service (5 replicas)                   │  │
│  │  │  └─ 核心业务逻辑、数据管理、用户认证                 │  │
│  │  ├─ Document Service (3 replicas)                      │  │
│  │  │  └─ 文档管理、版本控制、协作                         │  │
│  │  └─ Export Service (2 replicas)                        │  │
│  │     └─ PDF/Word导出、格式转换                           │  │
│  │                                                          │  │
│  │  Python服务 (FastAPI, Port 8001):                      │  │
│  │  ├─ AI Service (3 replicas)                            │  │
│  │  │  └─ LLM调用、向量检索、内容生成                      │  │
│  │  ├─ NLP Service (2 replicas)                           │  │
│  │  │  └─ 文档解析、需求分析、智能匹配                     │  │
│  │  └─ AI Worker Service (2 replicas)                    │  │
│  │     └─ 异步AI任务处理(Celery)                           │  │
│  │                                                          │  │
│  │  前端服务:                                               │  │
│  │  └─ Frontend (Nginx) (3 replicas)                      │  │
│  │     └─ 静态资源、SPA路由                                 │  │
│  └────────────────────┬───────────────────────────────────┘  │
│                       │                                        │
│  ┌────────────────────▼───────────────────────────────────┐  │
│  │  Data Services (StatefulSets)                           │  │
│  │  ├─ PostgreSQL (Primary + 2 Replicas)                  │  │
│  │  │  └─ Java服务数据存储                                 │  │
│  │  ├─ Redis (Cluster Mode, 6 nodes)                      │  │
│  │  │  └─ 缓存、会话、消息队列                             │  │
│  │  ├─ RabbitMQ (3 nodes)                                 │  │
│  │  │  └─ Java与Python服务间异步通信                       │  │
│  │  └─ Elasticsearch (3 nodes)                            │  │
│  │     └─ 向量存储、全文检索(Python AI服务)               │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
                               │
┌──────────────────────────────▼────────────────────────────────┐
│                    External Services                           │
│  ├─ MinIO / AWS S3 (Object Storage)                           │
│  ├─ Elasticsearch Cloud (可选云端向量存储)                     │
│  ├─ Pinecone (备选向量数据库)                                  │
│  ├─ Neo4j Cloud (知识图谱)                                     │
│  ├─ OpenAI API (LLM Service)                                  │
│  ├─ Prometheus + Grafana (监控)                               │
│  └─ ELK Stack (日志聚合)                                       │
└────────────────────────────────────────────────────────────────┘

服务间通信架构:
┌─────────────────┐                           ┌─────────────────┐
│   Java Service  │◄─────── HTTP REST ───────►│  Python Service │
│  (Spring Boot)  │                           │    (FastAPI)    │
│   Port: 8080    │                           │   Port: 8001    │
└────────┬────────┘                           └────────┬────────┘
         │                                              │
         └──────────► RabbitMQ ◄──────────────────────┘
                (异步任务、事件通知)
```

## 🎯 部署策略

### 开发环境

- **工具**: Docker Compose
- **特点**: 快速启动、易于调试
- **适用**: 本地开发、功能验证

### 测试环境

- **工具**: Kubernetes (Minikube / K3s)
- **特点**: 接近生产环境、支持集成测试
- **适用**: 功能测试、自动化测试

### 生产环境

- **工具**: Kubernetes集群（云平台 / 自建）
- **特点**: 高可用、自动扩缩容、故障恢复
- **适用**: 正式运行、大规模用户访问

## 📊 资源规划

### 最小配置（开发/测试）

| 服务 | CPU | 内存 | 存储 | 副本数 |
|------|-----|------|------|--------|
| Java后端 | 1核 | 2GB | - | 1 |
| Python AI | 2核 | 4GB | - | 1 |
| 前端 | 0.5核 | 512MB | - | 1 |
| PostgreSQL | 1核 | 2GB | 50GB | 1 |
| Redis | 0.5核 | 1GB | 10GB | 1 |
| Elasticsearch | 2核 | 4GB | 100GB | 1 |
| **总计** | **7核** | **13.5GB** | **160GB** | - |

### 推荐配置（生产）

| 服务 | CPU | 内存 | 存储 | 副本数 |
|------|-----|------|------|--------|
| Java后端 | 2核×5 | 4GB×5 | - | 5 |
| Python AI | 4核×3 | 8GB×3 | - | 3 |
| AI Worker | 2核×2 | 4GB×2 | - | 2 |
| 前端 | 0.5核×3 | 512MB×3 | - | 3 |
| PostgreSQL | 4核×3 | 16GB×3 | 500GB×3 | 3 |
| Redis | 2核×6 | 4GB×6 | 50GB×6 | 6 |
| Elasticsearch | 4核×3 | 16GB×3 | 500GB×3 | 3 |
| RabbitMQ | 2核×3 | 4GB×3 | 50GB×3 | 3 |
| **总计** | **82核** | **246GB** | **3.3TB** | - |

## 🚀 快速开始指南

### 开发环境快速启动

```bash
# 1. 克隆代码
git clone https://github.com/your-org/aibidcomposer.git
cd aibidcomposer

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填写必要的配置

# 3. 启动所有服务（Docker Compose）
docker-compose up -d

# 4. 查看服务状态
docker-compose ps

# 5. 查看日志
docker-compose logs -f

# 6. 访问应用
# 前端: http://localhost:5173
# Java API: http://localhost:8080
# Python AI API: http://localhost:8001
# API文档: http://localhost:8080/swagger-ui.html
```

### 生产环境部署流程

```bash
# 1. 准备Kubernetes集群
# 确保kubectl已配置并连接到目标集群

# 2. 创建命名空间和配置
kubectl create namespace aibidcomposer
kubectl apply -f k8s/configmaps/
kubectl apply -f k8s/secrets/

# 3. 部署数据服务
kubectl apply -f k8s/statefulsets/

# 4. 等待数据服务就绪
kubectl wait --for=condition=ready pod -l app=postgres -n aibidcomposer --timeout=300s
kubectl wait --for=condition=ready pod -l app=redis -n aibidcomposer --timeout=300s

# 5. 部署应用服务
kubectl apply -f k8s/deployments/

# 6. 部署Ingress和HPA
kubectl apply -f k8s/ingress/
kubectl apply -f k8s/hpa/

# 7. 验证部署
kubectl get pods -n aibidcomposer
kubectl get svc -n aibidcomposer
kubectl get ingress -n aibidcomposer
```

## 🔄 滚动更新策略

### 应用更新流程

```bash
# 1. 构建新版本镜像
./scripts/build-images.sh v1.2.0

# 2. 推送镜像到仓库
docker push registry.example.com/aibidcomposer/backend-java:v1.2.0
docker push registry.example.com/aibidcomposer/backend-python:v1.2.0
docker push registry.example.com/aibidcomposer/frontend:v1.2.0

# 3. 更新Kubernetes部署
kubectl set image deployment/backend-java \
  backend-java=registry.example.com/aibidcomposer/backend-java:v1.2.0 \
  -n aibidcomposer

# 4. 监控滚动更新进度
kubectl rollout status deployment/backend-java -n aibidcomposer

# 5. 验证新版本
curl https://api.aibidcomposer.com/health
```

### 回滚策略

```bash
# 如果更新出现问题，立即回滚
kubectl rollout undo deployment/backend-java -n aibidcomposer

# 查看回滚历史
kubectl rollout history deployment/backend-java -n aibidcomposer

# 回滚到特定版本
kubectl rollout undo deployment/backend-java --to-revision=3 -n aibidcomposer
```

## 📈 扩容策略

### 手动扩容

```bash
# 扩容Java后端服务到10个副本
kubectl scale deployment backend-java --replicas=10 -n aibidcomposer

# 扩容Python AI服务到5个副本
kubectl scale deployment backend-python --replicas=5 -n aibidcomposer
```

### 自动扩容（HPA）

详见 `02-Kubernetes配置.md` 中的HPA配置。

## 🔍 监控检查点

### 服务健康检查

| 服务 | 健康检查URL | 预期响应 |
|------|-------------|----------|
| Java后端 | http://localhost:8080/actuator/health | {"status":"UP"} |
| Python AI | http://localhost:8001/health | {"status":"healthy"} |
| 前端 | http://localhost/health | "healthy" |
| PostgreSQL | `pg_isready -U postgres` | 正常退出 |
| Redis | `redis-cli ping` | PONG |
| Elasticsearch | http://localhost:9200/_cluster/health | green/yellow |

## 🎯 下一步

- **Docker配置**: 参见 [01-Docker配置详解.md](./01-Docker配置详解.md)
- **Kubernetes部署**: 参见 [02-Kubernetes配置.md](./02-Kubernetes配置.md)
- **运维脚本**: 参见 [03-部署运维脚本.md](./03-部署运维脚本.md)

---

## 修改历史

| 日期 | 版本 | 修改者 | 修改内容概要 |
|------|------|--------|-------------|
| 2025-11-29 | 1.0 | claude-sonnet-4-5 (claude-sonnet-4-5-20250929) | 从06-部署架构设计.md拆分创建部署架构总览文档 |

---

**文档版本**: v1.0
**创建时间**: 2025年11月29日
**文档状态**: ✅ 已批准
